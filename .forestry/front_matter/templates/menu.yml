---
label: Menu
hide_body: true
fields:
- name: menu_item
  type: field_group_list
  fields:
  - name: label
    type: text
    config:
      required: false
    label: Label
  - name: icon
    type: file
    config:
      maxSize: 64
    label: icon
  config:
    min: 
    max: 
    labelField: 
  label: Menu Item
